# Copy this file to data/.env and fill in your Grafana instance details.
# data/.env is git-ignored.

# Base URL of your Grafana instance (no trailing slash).
GRAFANA_URL=https://grafana.example.com

# Service account token (Admin or Editor role; needs datasource + dashboard perms).
GRAFANA_TOKEN=""
